{
  "version": 1,
  "ports": { "preview": 3000, "additional": {} },
  "scripts": {
    "setup": {
      "enabled": true,
      "command": "bun install && apt-get update -qq && apt-get install -y -qq git-lfs"
    },
    "run": { "enabled": true, "command": "bun run start" },
    "archive": { "enabled": true, "command": null },
    "check": { "enabled": true, "command": null }
  },
  "mcpServers": []
}
